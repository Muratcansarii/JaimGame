namespace JAIM.Control // this namespace holds attributes about control
{
    public enum CursorType{ // defining an enum cursortype with attributes
            None,
            Movement,
            Combat,
            UI,
            Pickup,
        }
}